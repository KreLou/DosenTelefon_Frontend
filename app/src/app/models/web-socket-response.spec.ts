import { WebSocketResponse } from './web-socket-response';

describe('WebSocketResponse', () => {
  it('should create an instance', () => {
    expect(new WebSocketResponse()).toBeTruthy();
  });
});
