import { WebSocketConnectionItem } from './web-socket-connection-item';

describe('WebSocketConnectionItem', () => {
  it('should create an instance', () => {
    expect(new WebSocketConnectionItem()).toBeTruthy();
  });
});
